#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 *
 * @c: The value to be computed
 *
 * Return: Absolute value or 0
 */
int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (-c);
}
